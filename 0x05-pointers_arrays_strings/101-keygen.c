#include <stdio.h>

/**
 * program generates random valid passwords
 * @s: Password
 * Return (0)
 */

int main ( int argc, char *argv[])
{
	if (argc != 2)
	{
		printf("Usage: %s password", argv[0]);
	}
	else
	{
		if (condition to check for the password)
			puts("Wrong password");
		else
			prints("Tada Congrats!");
	}
}
